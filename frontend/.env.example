# Database / backend
# NOTE: In production, always set NEXT_PUBLIC_API_URL to an https:// URL.
# Never rely on the http://localhost:8000 fallback in production environments.
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_URL=http://localhost:3000
